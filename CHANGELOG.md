# 变更记录

## 1.3.0 - 2026-08-26

- 对齐上游数据库句柄失效通知和按数据库名称刷新重试，兼容 `@kefu.openim` 联系人查询。
- 增加原生 API 控制请求和消息发送请求的有限超时，分别支持环境变量覆盖。
- Bridge 在微信未登录时暂缓取消息；无租约的旧响应保留有限分发重试。
- 增加 `MarkAsRead` API 和 49 号请求模型；仅提供调用接口，不会自动标记微信消息已读。
- 保留现有持久化队列、ACK/NACK、去重、回执数据库、TCP 回调和 Bridge 双通道定制。

## 1.2.0 - 2026-07-31

- Bridge 拉取启用租约模式，消息分发成功后 ACK，失败后 NACK。
- 新增 SQLite WAL 消费回执，ACK 丢失后补确认而不重复分发。
- 保持不含租约元数据的旧 Bridge 响应兼容。
- 新增消费者名称、回执路径和回执保留时间配置。

## 1.1.0 - 2026-07-30

- 保留原稳定 TCP 回调作为默认消息模式。
- 移植上游 Bridge API 长轮询接收能力。
- 新增 `tcp`、`bridge`、`auto` 三种启动模式。
- 自动模式仅在启动时选择一次，避免双通道造成重复消息。
- 保留可配置的 ComWechat API 端口和回调端口。
- 保留 `sent_msg` 事件与群成员 SQL 扩展。
- 增加 Bridge 响应校验、TCP 分帧、后台线程关闭能力。
- 修复事件装饰器未返回原函数的问题。
- 增加 Python 3.8/3.11 自动测试和发行包构建检查。
